<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HaircutHistoryApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:HaircutHistoryApp.Views"
    Title="Haircut History"
    FlyoutWidth="280"
    FlyoutBehavior="Flyout"
    Shell.BackgroundColor="{StaticResource Primary}"
    Shell.ForegroundColor="White"
    Shell.TitleColor="White"
    FlyoutBackgroundColor="{AppThemeBinding Light={StaticResource BackgroundLight}, Dark={StaticResource BackgroundDark}}">

    <!-- Flyout Header -->
    <Shell.FlyoutHeader>
        <Grid BackgroundColor="{StaticResource Primary}"
              Padding="20"
              HeightRequest="140">
            <HorizontalStackLayout VerticalOptions="Center" Spacing="12">
                <Image Source="icon.png"
                       HeightRequest="50"
                       WidthRequest="50"/>
                <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                    <Label Text="Haircut History"
                           FontSize="22"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Never forget your perfect cut"
                           FontSize="12"
                           TextColor="White"
                           Opacity="0.8"/>
                </VerticalStackLayout>
            </HorizontalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <!-- Flyout Item Template -->
    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid Padding="20,15">
                <Label Text="{Binding Title}"
                       FontSize="16"
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}"/>
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <!-- Login (flyout hidden) -->
    <ShellContent Route="login"
                  ContentTemplate="{DataTemplate views:LoginPage}"
                  Shell.FlyoutBehavior="Disabled"
                  Shell.FlyoutItemIsVisible="False"
                  Shell.TabBarIsVisible="False"
                  Shell.NavBarIsVisible="False"/>

    <!-- Main Router (flyout hidden) -->
    <ShellContent Route="main"
                  ContentTemplate="{DataTemplate views:MainPage}"
                  Shell.FlyoutBehavior="Disabled"
                  Shell.FlyoutItemIsVisible="False"
                  Shell.TabBarIsVisible="False"
                  Shell.NavBarIsVisible="False"/>

    <!-- Main App with Flyout Menu -->
    <FlyoutItem Title="My Profiles" Route="profiles">
        <ShellContent ContentTemplate="{DataTemplate views:ProfileListPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Scan QR Code" Route="scan">
        <ShellContent ContentTemplate="{DataTemplate views:QRScanPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Shared With Me" Route="shared">
        <ShellContent ContentTemplate="{DataTemplate views:SharedProfilesPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Glossary" Route="glossary">
        <ShellContent ContentTemplate="{DataTemplate views:GlossaryPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Themes" Route="themes">
        <ShellContent ContentTemplate="{DataTemplate views:ThemeSelectionPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Premium" Route="premium">
        <ShellContent ContentTemplate="{DataTemplate views:PremiumPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Settings" Route="settings">
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}"/>
    </FlyoutItem>

</Shell>
